// A interface declara um typo igual o metodo tambem usado que é o type.
// A diferença de interface e type é que em type usamos o sinal de igual (=) e em interface não é utilzado.
interface User {
     nome: string, 
     idade: number 
    }

let usuario: User = {
    nome: "joao",
    idade: 27
}